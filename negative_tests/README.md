# negative_tests

Source files from both [`sneaky_unsafe_stops_compilation/`](sneaky_unsafe_stops_compilation/) and
[`unused_unsafe_fails_lint/`](unused_unsafe_fails_lint/) get loaded by
[`../src/doctests/`](../src/doctests/).

In addition to that, source files from [`unused_unsafe_fails_lint/`](unused_unsafe_fails_lint/) get
loaded by [`verify_error_messages/`](verify_error_messages/).
