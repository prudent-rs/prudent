//! User-facing functionality suggested to be imported by the user. User-facing macros.

pub use crate::{
    unsafe_fn, unsafe_method, unsafe_mut, unsafe_ref, unsafe_set, unsafe_static_set, unsafe_val,
};
